[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "edge-video"
version = "0.1.0"
description = "Raspberry Pi video evidence service for the AI Legal Edge platform"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
